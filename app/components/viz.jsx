let React = require('react');
let mui = require('material-ui');

let Viz = React.createClass({
  tagName() { return 'Viz'; },
  render() {
    return (
      <div>
        I will be the viz!
      </div>
    );
  },
});

module.exports = Viz;
